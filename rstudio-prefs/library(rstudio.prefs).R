# https://www.danieldsjoberg.com/rstudio.prefs/

if(!require("rstudio.prefs") ) { install.packages("rstudio.prefs") }

library(rstudio.prefs)
# pref_list <- list(editor_scroll_multiplier = 30L)
# use_rstudio_prefs(!!!pref_list)   # note the !!! to splice the list
